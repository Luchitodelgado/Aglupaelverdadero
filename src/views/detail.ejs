<!DOCTYPE html>
<html lang="en">

<%- include('partials/head') %>


	<body>
		<%- include('partials/headerNavbar') %>
			<main>
				<h1 class="h1-detail">Detalle del producto</h1>
				<div class="container-detail">
					<img class="img-detail" src="/img/<%= product.image %>" alt="product image">
					<p class="name-detail">
						<%= product.name %>
					</p>
					<p class="description-detail">
						<%= product.description %>
					</p>
					<% if(product.discount> 0){
						let finalPrice = (product.price * ((100 - product.discount) / 100)).toFixed(0)
						%>
						<p class="price-detail">$<%= finalPrice %>
						</p>
						<% } else { %>
							<%= product.price %>
								<% } %>
								<p>
									<i class="fas fa-credit-card"></i>
									Pagá en 12 cuotas sin interés
								</p>
									<i class="fas fa-store"></i>
									<p>Retiro gratis en locales del vendedor</p>
									<a href="#">COMPRAR AHORA</a>
									<a href="/productos/edit/<%= product.id %>">EDITAR PRODUCTO</a>
									<form action="/productos/delete/<%= product.id %>?_method=DELETE" method="POST"
										style="display: inline-flex">
										<button type="submit">ELIMINAR</button>
									</form>
				</div>
			</main>
			<%- include('partials/footer') %>
	</body>

</html>