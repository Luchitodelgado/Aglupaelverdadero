<!DOCTYPE html>
<html lang="en">
<%- include('partials/head') %>

  <body>
    <%- include('partials/headerNavbar') %>
      <main>
        <h1 class="h1-formulario">Ingresá</h1>
        <div class="contenedor-padre">
          <!-----------------FORM--------------------->
          <form action="/login" method="POST" class="loginForm" id="formularioLogin">
            <!-----------------EMAIL--------------------->
            <div class="login formulario__grupo" id="grupo__email">
              <label for="email" class="formulario__label">
                <p class="p-login">EMAIL</p>
              </label>
              <div>
                <input class="input-hijo formulario__input" type="email" name="email" id="loginEmail"
                  placeholder="email@email.com"></input>
                <i class="formulario__validacion-estado fas fa-times-circle"></i>
              </div>
              <% if (locals.errors && errors.email) { %>
                <%= errors.email.msg %>
                  <%}%>
                    <p class="formulario__input-error">El email solo puede contener letras, numeros, puntos, guiones y
                      guion
                      bajo.</p>
                    <% if (locals.mailEnUso) { %>
                      <p> Porfavor ingrese otro email.</p>
                      <% } %>
            </div>
            <!-----------------PASSWORD--------------------->
            <div class="login formulario__grupo" id="grupo__password">
              <label for="password" class="formulario__label">
                <p class="p-login">CONTRASEÑA</p>
              </label>
              <div class="formulario__grupo-input">
                <input class="input-hijo formulario__input" type="password" name="password" id="loginPassword"
                  placeholder="Contraseña"></input>
                <i class="formulario__validacion-estado fas fa-times-circle"></i>
              </div>
              <% if (locals.errors && errors.password) { %>
                <%= errors.password.msg %>
                  <%} %>
                    <p class="formulario__input-error">La contraseña tiene que ser de 4 a 12 dígitos.</p>
            </div>

            <div>
              <input type="checkbox" name="recordame"> Recordame</input>

            </div>

            <div class="formulario__mensaje" id="formulario__mensaje">
              <p><i class="fas fa-exclamation-triangle"></i> <b>Error:</b> Por favor rellena el formulario
                correctamente. </p>
            </div>

            <div class="formulario__grupo formulario__grupo-btn-enviar">
              <button type="submit" class="formulario__btn">Enviar</button>
              <p class="formulario__mensaje-exito" id="formulario__mensaje-exito">Formulario enviado exitosamente!</p>
            </div>
          </form>
        </div>
      </main>
      <script src="../../js/loginForm.js"></script>
      <%- include('partials/footer') %>
  </body>

</html>